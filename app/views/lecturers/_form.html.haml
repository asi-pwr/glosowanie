= form_for(lecturer) do |f|
  - if lecturer.errors.any?
    #error_explanation
      %h2
        = pluralize(lecturer.errors.count, "error")
        prohibited this lecturer from being saved:
      %ul
        - lecturer.errors.full_messages.each do |message|
          %li= message
  .field
    = f.label :full_name
    = f.text_field :full_name
  .actions
    = f.submit
